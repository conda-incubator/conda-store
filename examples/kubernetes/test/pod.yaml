apiVersion: batch/v1
kind: Job
metadata:
  name: example-job
spec:
  template:
    spec:
      containers:
      - name: hello
        image: localhost:30500/default/test:latest
        command: ['flask', '--help']
      restartPolicy: OnFailure
